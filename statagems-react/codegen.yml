overwrite: true
schema: "http://127.0.0.1:5000/graphql"
documents: "src/**/*.graphql"
generates:
  src/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
