query GetMatchList {
  matches {
    ...matchList
  }
}

fragment matchList on Match {
  datePlayed
  id
  score
  map {
    id
    mapName
  }
  teams {
    id
    matchPlayers {
      id
      playerId
      avatar {
        mediumAvatarUrl
      }
    }
  }
}
